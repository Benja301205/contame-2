/**
 * Acento de marca para gráficos (Recharts no lee variables CSS de shadcn
 * directo en sus props de color). Mismo verde-lima que --chart-1 en
 * app/globals.css — mantenerlos sincronizados si se cambia la paleta.
 */
export const CHART_ACCENT = "#65a30d";
